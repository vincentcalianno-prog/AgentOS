"""Layer C Antora cells and config for the report triage workflow.

Contains:
    reports/  — per-report YAML config (column maps, grain, thresholds)
    owners.yaml — buyer roster and owner-fallback rules
    sheets_config.yaml — Google Sheets workbook and tab config
    actions/  — action executor cells (buyer email, system updates)
    storage/  — concrete storage adapter (Google Sheets)
"""
