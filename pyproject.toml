[project]
name = "acp"
version = "0.1.0"
description = "Agent Contract Platform — automated contract negotiation pipeline"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["acp*"]
